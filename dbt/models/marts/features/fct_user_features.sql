-- Feature mart placeholder: user-level batch features for Feast.

select
    cast(null as text) as customer_id,
    cast(null as integer) as user_total_orders,
    cast(null as numeric) as user_avg_order_value
where false
